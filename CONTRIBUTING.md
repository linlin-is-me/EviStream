# Contributing

Use a focused branch and Conventional Commits. Run the backend and frontend checks before
opening a pull request. Do not commit credentials, model caches, unlicensed media, or generated
benchmark claims without their source configuration.

Stage branches use `stage/<number>-<topic>`. Feature work should include tests in the same commit.

